# generated at 1781111709.6655931
project_name: "tableau_migration"

# Tableau parameters are surfaced here as project constants so they are
# discoverable. Wire them into filters / templated_filters as needed.
constant: top_customers {
  value: "5"
  # Tableau parameter "Top Customers" (integer)
}
constant: profit_bin_size {
  value: "200"
  # Tableau parameter "Profit Bin Size" (integer)
}
constant: select_year {
  value: "2025"
  # Tableau parameter "Select Year" (integer)
}
